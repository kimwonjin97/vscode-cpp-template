{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with build.bat",
            "type": "shell",
            "command": "${workspaceFolder}\\build.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            
            "problemMatcher": [
                "$msCompile",
            ]
        }
    ]
}